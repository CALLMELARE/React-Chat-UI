import React from "react";
import VideoCall from ".";

export default {
  title: "页面组件/VideoCall(视频会议)",
  component: VideoCall,
};

export const Default = () => (
  <div style={{ height: "100vh" }}>
    <VideoCall />
  </div>
);
